{
  "name": "cnc-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CNC-first web front end for RepRapFirmware (and other) controllers",
  "scripts": {
    "dev": "node build.mjs --watch --serve",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
